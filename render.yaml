services:
  - type: worker
    name: tg-bot
    env: python
    plan: hobby
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot_run.py"
    autoDeploy: true
    envVars:
      - key: TG_TOKEN
        sync: false